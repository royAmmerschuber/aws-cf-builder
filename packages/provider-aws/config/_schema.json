{
    "$schema": "http://json-schema.org/draft-07/schema",
    "definitions": {
        "generatable":{
            "description": "describes a resource and its references to other resources",
            "type":"object",
            "properties": {
                "alias":{
                    "type":"string",
                    "description": "an Alias for the resource, incase the name overlaps with a global constant or any other important part"
                },
                "nameParts":{
                    "type":"array",
                    "items": {"type":"string"},
                    "description": "the attributes that make up the autogenerated name. All of the properties that you specify here will be asked by the constructor unless provided by a parent resource.\n use '_alias' if you want to add a value that is just used for the name generation"
                },
                "childResources":{
                    "type":"object",
                    "additionalProperties": {
                        "type":"array",
                        "items": {"type":"string"}
                    },
                    "description": "the resources wich are subresources of this resource.\nfor example with the subresource aws_lambda_event_source_mapping write:\n\n{\n    \"lambda\":[\"event_source_mapping\"]\n}\n\nlambda being the resource group and the list containing the resource from the group.\nyou can specify however many you want"
                },
                "provides":{
                    "type":"boolean",
                    "description":"if this resource should pass itself down to its children"
                },
                "inherits":{
                    "type":"object",
                    "additionalProperties": {
                        "type":"object",
                        "items": {"type":"string"},
                        "properties": {
                            "resource":{
                                "type":"string",
                                "description": "the full resource identifier of the inherited Resource"
                            },
                            "attribute":{
                                "type":"string",
                                "description": "the Attribute that will be inherited from the parent resource"
                            }
                        },
                        "required": ["resource","attribute"]
                    },
                    "description": "maps parent resources to the attributes in this resource\n\nthe key is the argument to wich it will be mapped.\nthe setter-method for the arguments here will be removed."
                }
            },
            "additionalProperties": false

        }
    },
    "properties": {
        "resources":{
            "description": "the resources with their respective keys. the resource group is specified by the filename, so you just need to specify the subname for example with the aws_lambda_function you just need to specify function the case must be snake_case",
            "type":"object",
            "additionalProperties":{"$ref": "#/definitions/generatable"} 
        },
        "datasources":{
            "description": "the resources with their respective keys. the resource group is specified by the filename, so you just need to specify the subname for example with the aws_lambda_function you just need to specify function the case must be snake_case",
            "type":"object",
            "additionalProperties":{"$ref": "#/definitions/generatable"}
        }
    },
    "required": ["resources","datasources"]
}