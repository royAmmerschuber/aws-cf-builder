{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "test jest",
            "program": "${workspaceFolder}/node_modules/.bin/jest",
            "args":["--runInBand"],
            "console": "integratedTerminal",
            "windows": {
                "program":"${workspaceFolder}/node_modules/jest/bin/jest"
            }
        },
        {
            "type": "node",
            "request": "launch",
            "name":"testConst",
            "runtimeArgs":["--no-lazy","-r","ts-node/register"],
            "args": ["${workspaceRoot}/debugTransforms.ts"],
            "cwd":"${workspaceFolder}"
        }
    ]
}